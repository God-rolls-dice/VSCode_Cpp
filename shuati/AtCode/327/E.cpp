#include <bits/stdc++.h>
using namespace std;

void solve()
{
    int N;
    cin >> N;
    vector<int> Q(N + 1, 0);
    for (int i = 1; i <= N; i++)
    {
        cin >> Q[i];
    }
    
}

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    int t = 1;
    while(t--)
    {
        solve();
    }
}