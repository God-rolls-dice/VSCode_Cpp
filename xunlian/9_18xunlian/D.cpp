#include <bits/stdc++.h>
using namespace std;

void solve()
{
    int n, temp, up_l, up_r, low_l, low_r;
    cin >> n;
    vector<int> a(1, 0);
    for (int i = 1; i <= n; i ++)
    {
        cin >> temp;
        a.push_back(temp);
    }
    
}

int main()
{
    int t = 0;
    cin >> t;
    while (t--)
    {
        solve();
    }
}